Create lib	[libft]
	Modules:
		{memory}
		{string}
		{char}
		{xtoy}
		{output}
		{list}
		{dlist}
		{cdlist}
		{math}

Rewrite atoi without using OVERFLOW macros
Write math part of libft
Add dlist (double linked) list
Add cdlist (circular double linked) list
Add del function for all lists
Add del function for args[][]